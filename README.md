# Campus Event Management Website

A collaborative project to manage and showcase university events.

## Project Structure
- **src/**: Contains HTML source files.
- **styles/**: Contains shared CSS.
- **dist/**: Build output (ignored in git).

## How to Run Locally
1. Install dependencies:
   ```bash
   npm install